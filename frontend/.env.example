# Next.js Environment Variables
# Copy this file to .env.local and update the values

# Application
NEXT_PUBLIC_APP_NAME="Electricity Theft Detection System"
NEXT_PUBLIC_APP_VERSION="1.0.0"

# Data Paths (relative to frontend directory)
# Leave as default if your outputs folder is in the parent directory
DATA_PATH="../outputs"

# Optional: API Configuration
# NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Optional: Feature Flags
# NEXT_PUBLIC_ENABLE_ALERTS=true
# NEXT_PUBLIC_ENABLE_EXPORTS=true

# Optional: Refresh Intervals (in milliseconds)
# NEXT_PUBLIC_REFRESH_INTERVAL=30000
